version: '3.1'

services:
  db:
    image: store/oracle/database-enterprise:12.2.0.1-slim
    environment:
      DB_SID: ORCLCDB
    volumes:
      - oracle-db-data:/ORCL
    ports:
      - 1521:1521
    networks:
      - default
      - global
      
volumes:
  oracle-db-data:

networks:
  global:
    external: true

# defautl username/password: sys/Oradoc_db1